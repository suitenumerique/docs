/**
 * Height of the row holding the Docs logo. Shared by the left panel header and
 * the standalone page header so the logo does not shift between pages.
 */
export const HEADER_ROW_MIN_HEIGHT = '64px';
